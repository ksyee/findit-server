# Disable API key security for tests unless explicitly enabled
API_SECURITY_ENABLED=false
security.api.enabled=false

# Provide noop values to satisfy configuration binding when tests enable security
API_SECURITY_KEY=test-key
security.api.key=test-key
security.api.header-name=X-API-KEY
