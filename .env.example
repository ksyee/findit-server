# 기본 환경 변수 예시 (배포 시 값은 안전한 방식으로 주입하세요)

# Spring 프로필
SPRING_PROFILES_ACTIVE=docker

# PostgreSQL 접속 정보 (docker-compose, AWS RDS 등)
SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/findit
SPRING_DATASOURCE_USERNAME=findit
SPRING_DATASOURCE_PASSWORD=change_me

# 스케줄링/외부 연동 플래그
POLICE_API_ENABLED=false
POLICE_API_SERVICE_KEY=

# Docker Compose용 PostgreSQL 기본값 (선택)
POSTGRES_DB=findit
POSTGRES_USER=findit
POSTGRES_PASSWORD=change_me

# 애플리케이션 포트
SERVER_PORT=8080
